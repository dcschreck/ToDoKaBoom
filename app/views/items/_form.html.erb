<%= form_for [item] do |f| %>
    <div class="col-md-6">
        <p>Reminders for Your To-Dos</p>
        <ul>
            <li>Each To-Do will disappear from your list if you don't complete it!</li>
            <li>Anyone can see your To-Do list...peer pressure is a great motivator!</li>
            <li>Life is busy, but this tool can help you accomplish your goals.</li>
        </ul>
    </div>
    <div class="col-md-6">
        <%= form_for @item do |f| %>
            <div class="form-group">
                <%= f.label :name %>
                <%= f.text_field :name, class: 'form-control', placeholder: "Enter your To-Do" %>
            </div>
            <div class="form-group">
                <%= f.submit "Save", class: 'btn btn-success'%>
            </div>
        <% end %>
    </div>
<% end %>
